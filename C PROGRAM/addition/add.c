//this program is used to add two number
#include <stdio.h>
int main()
{
    int first, second;
    printf("enter number to add ");
    scanf("%d", &first);
    printf("enter another number to add ");
    scanf("%d", &second);
    printf("the sum of number is %d", first + second);

    return 0;
}